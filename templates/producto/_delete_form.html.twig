<form method="post" class="delete-form mt-2" action="{{ path('producto_delete', {'id': producto.id}) }}">

    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-danger"><span class="fas fa-trash-alt icon_menu"></span> Borrar</button>
</form>
