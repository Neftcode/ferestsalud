{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}} - Inventario PEPS - Ver
{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compra.id }}</td>
            </tr>
            <tr>
                <th>Fecha compra</th>
                <td>{{ compra.fechaCompra }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compra_index') }}" class="btn btn-warning"><span class="fas fa-arrow-left icon_menu"></span> Regresar</a>
    <a href="{{ path('compra_edit', {'id': compra.id}) }}" class="btn btn-ferest"><span class="fas fa-pen icon_menu"></span> Editar</a>

    {{ include('compra/_delete_form.html.twig') }}
{% endblock %}
